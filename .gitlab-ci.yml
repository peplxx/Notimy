stages:
  - test

variables:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_PORT: 5432
  POSTGRES_DB: postgres
  POSTGRES_HOST: localhost

test_backend:
  stage: test
  script:
    - cd backend
    - poetry install
    - make test
